radius: float = float(input("Enter the radius of the circle : "))
pi: float = 3.14
e: float = 1.0
p: float = 0.0
area: float

if radius == 0:
    print("Circle doesn't exist")
elif radius <= p:
    print("Circle doesn't exist")

area = pi * radius * radius
print(area)
